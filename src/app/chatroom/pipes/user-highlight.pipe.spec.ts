import { UserHighlightPipe } from './user-highlight.pipe';

describe('UserHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new UserHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
